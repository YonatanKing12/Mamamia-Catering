/**
 * ═══════════════════════════════════════════════════════════════════════
 *  ההוכחה — ביקורות, המלצות וצילומי אירועים. כולה `null` היום.
 * ═══════════════════════════════════════════════════════════════════════
 *
 * לקרוא קודם את בלוק המיצוב בראש `content/business.ts`.
 *
 * בקטגוריה הזאת האמון נישא בשלושה דברים: גוף כשרות נקוב, מונה ביקורות
 * גוגל גלוי עם דירוג, וצילומים של אירועים אמיתיים. שניים מהשלושה חיים
 * בקובץ הזה, והשלישי (`SLOTS.kashrutByBranch`) חי ב־`business.ts`.
 *
 * ─────────────────────────────────────────────────────────────────────
 *  החוק
 * ─────────────────────────────────────────────────────────────────────
 * דירוג, מונה ביקורות, ציטוט בשם אדם ותצלום של אירוע הם **ראיות**, לא
 * קישוט. ראיה מומצאת גרועה מהיעדר ראיה בשלושה מובנים בבת אחת:
 *
 *   · **צרכנית.** «4.9 · 312 ביקורות» שאין מאחוריו פרופיל הוא מצג שווא.
 *   · **משפטית.** ציטוט בשם אדם ותמונה שרואים בה אנשים הם מידע אישי.
 *     פרסום בלי הרשאה בכתב הוא הפרה, וההרשאה נדרשת **לפני** ההעלאה ולא
 *     אחרי שמישהו יתלונן.
 *   · **תפעולית.** גוגל אוסר קטעי ביקורת שהעסק כתב על עצמו עבור
 *     `LocalBusiness`, ו־`lib/seo.ts` מוחק בכוח `aggregateRating`,
 *     `review` ו־`ratingValue` מכל JSON-LD. כלומר גם אם מישהו יכתוב
 *     כאן מספר — הוא לא יגיע לנתונים המובנים, אבל כן יגיע למסך.
 *
 * לכן: כל משבצת כאן `null` או `[]`, וכל בורר מחזיר `null` / `[]` /
 * `false` במצב הריק. `components/trust/*` בודקים את הבוררים לפני שהם
 * מרנדרים משהו, וסקשן שאין לו ולו פריט אחד — **אינו מרונדר כלל**.
 * אין דירוג לדוגמה, אין «עוד לא התקבלו ביקורות», ואין ריבוע אפור שממתין
 * לתמונה. העמוד נראה גמור כשהקובץ ריק, כי זה מצבו היום.
 *
 * ─────────────────────────────────────────────────────────────────────
 *  למה אין כאן וידג'ט ביקורות של צד שלישי
 * ─────────────────────────────────────────────────────────────────────
 * אתר האסמכתה (`docs/spec/04-visual-reference.md` §5) מציג ביקורות גוגל
 * דרך Trustindex. אנחנו לא. וידג'ט כזה טוען סקריפט מדומיין זר בכל טעינת
 * דף, ומעביר לו כתובת IP, User-Agent ו־Referer של כל מבקר — כלומר מעביר
 * מידע אישי למעבד נוסף. דף הפרטיות שלנו מונה את המעבדים שלנו; מעבד
 * שאינו מנוי שם ופועל בכל זאת הוא סתירה בין מה שכתוב לבין מה שרץ.
 * הוא גם עוד סקריפט חוסם בעמוד שכל תפקידו להמיר תנועה בתשלום.
 *
 * הצורה הנכונה היא הנתונים כאן, בטקסט שלנו, עם קישור יוצא לפרופיל
 * הציבורי כדי שכל אחד יוכל לאמת בעצמו.
 *
 * ─────────────────────────────────────────────────────────────────────
 *  אין React ואין JSX כאן. מודול נתונים בלבד.
 * ─────────────────────────────────────────────────────────────────────
 */

import { filled, type Slot } from "@/content/business";

export { filled } from "@/content/business";
export type { Slot } from "@/content/business";

/* ═══════════════════ 1 · ביקורות גוגל ═══════════════════ */

/**
 * מונה הביקורות והדירוג — **של מי בדיוק**.
 *
 * זו לא שאלה פורמלית. לקייטרינג ולמסעדות יש פרופילים נפרדים, והדירוג
 * של מסעדה **אינו** הדירוג של הקייטרינג. «4.7 · 480 ביקורות» שנלקח
 * מפרופיל מסעדה ומוצג לצד כפתור «קבלו הצעה לקייטרינג» אומר לקורא דבר
 * אחד בלבד: 480 אנשים דירגו את הקייטרינג הזה. זו הטענה, וזו הסיבה
 * ש־`subjectHe` הוא שדה חובה ולא הערה.
 *
 * `subjectHe` מנוסח בידי הבעלים ומרונדר כלשונו. אם הפרופיל הוא של
 * מסעדה — הניסוח אומר את זה, ואז ההצגה כנה. אם הוא של הקייטרינג —
 * הניסוח אומר את זה. אין ברירת מחדל ואין ניסוח שהקוד ממציא.
 */
export interface GoogleReviewProfile {
  /**
   * הדירוג כפי שגוגל מציג אותו. ‎1–5, ספרה עשרונית אחת לכל היותר.
   * לא מעוגל כלפי מעלה, לא ממוצע של כמה פרופילים.
   */
  ratingValue: number;

  /** מספר הביקורות באותו פרופיל, במדויק. */
  reviewCount: number;

  /** כתובת הפרופיל הציבורי. בלעדיה אין דרך לאמת, ולכן אין הצגה. */
  profileUrl: string;

  /**
   * **מה דורג**, כלשון הבעלים. מרונדר. ראו ההסבר למעלה.
   * דוגמאות לניסוח תקין: «הביקורות על הקייטרינג בגוגל»,
   * «הביקורות על המסעדה בהרצליה פיתוח».
   */
  subjectHe: string;

  /**
   * התאריך שבו הועתקו המספרים (ISO ‎YYYY-MM-DD). מונה ביקורות הוא עובדה
   * שמשתנה; בלי תאריך אין דרך לדעת מתי הוא היה נכון, ואי אפשר לתזמן
   * רענון. מוצג בזעיר לצד הבלוק.
   */
  capturedOn: string;
}

/**
 * TODO(owner): קישור לפרופיל גוגל, הדירוג המדויק, מספר הביקורות המדויק,
 * וניסוח **מה** דורג. אין להעתיק מספר מזיכרון ואין לעגל.
 *
 * כשזה יגיע — `lib/seo.ts` מחזיק היום `aggregateRating` ו־`review`
 * ברשימת המפתחות האסורים, ובצדק (spec 01 §7.3). שחרורם הוא החלטה נפרדת
 * ומחייב לוודא שהפרופיל הוא של הישות שה־JSON-LD מתאר. הקובץ הזה אינו
 * משנה את `seo.ts` ואינו מניח שהוא ישתנה.
 */
export const GOOGLE_REVIEWS: Slot<GoogleReviewProfile> = null;

/* ═══════════════════ 2 · המלצות ═══════════════════ */

/**
 * רשומת ההסכמה. **בלעדיה אין רינדור**, בשום ניסוח ובשום מקום —
 * לא בעמוד, לא ב־JSON-LD, ולא בקרוסלה.
 *
 * «הלקוח אמר לי שזה בסדר בטלפון» אינו הסכמה שאפשר להראות למישהו.
 * הרשומה כאן היא מה שמאפשר להוכיח, בעוד שנתיים, שהפרסום הותר: מי אישר,
 * מתי, ואיפה שמור האישור.
 */
export interface PublishConsent {
  /** הסכמה מפורשת בכתב לפרסום באתר. `false` או `null` שקולים. */
  granted: boolean;
  /** מתי ניתנה (ISO ‎YYYY-MM-DD). בלי תאריך — אין רשומה. */
  grantedOn: Slot<string>;
  /**
   * איפה שמור האישור: «וואטסאפ, שרשור עם ההזמנה», «מייל מ־30.7.26»,
   * «טופס חתום בתיק הלקוח». זה מה שהופך את הרשומה לניתנת לאימות.
   */
  evidenceHe: Slot<string>;
}

export interface Testimonial {
  /** slug לטיני יציב. מפתח רשימה ומימד אנליטיקה — לא משתנה אחרי שנקבע. */
  id: string;

  /**
   * הציטוט **כלשונו**. לא מתומצת, לא «משופר», לא מתורגם מהמלצה בעל־פה.
   * מי שעורך ציטוט מייחס לאדם מילים שהוא לא אמר.
   */
  quoteHe: string;

  /**
   * איך האדם הסכים שייקרא לו — **בדיוק** כפי שהסכים. «רונית, אירוע
   * חברה» או «מנהלת משרד בחברת הייטק». שם מלא רק אם הוסכם שם מלא.
   * `null` ⇒ ההמלצה מוצגת בלי ייחוס, וזו אפשרות תקינה.
   */
  attributionHe: Slot<string>;

  /** סוג האירוע, אם ההסכמה כוללת אותו. לא נגזר ולא מנוחש. */
  eventTypeHe: Slot<string>;

  /**
   * מתי היה האירוע (ISO ‎YYYY-MM-DD). אופציונלי. אינו מוצג כברירת מחדל
   * — המלצה מתוארכת מיושנת מזיקה יותר מהמלצה בלי תאריך.
   */
  eventDate: Slot<string>;

  consent: PublishConsent;
}

/**
 * ריק, וזה המצב התקין.
 *
 * TODO(owner): לכל המלצה — הציטוט כלשונו, איך לייחס אותה, **ואישור
 * בכתב לפרסום** עם תאריך והיכן הוא שמור. המלצה בלי אישור אינה נכנסת
 * לקובץ הזה גם לא «זמנית»: מה שנכנס — עולה לאוויר.
 *
 * למי שממלא: אין להעתיק ביקורות מגוגל לכאן כדי «למלא». ציטוט של ביקורת
 * גוגל שמוצג באתר הוא עדיין פרסום של מילות אדם, ועדיין דורש הרשאה.
 */
export const TESTIMONIALS: readonly Testimonial[] = [];

/* ═══════════════════ 3 · גלריית אירועים ═══════════════════ */

/**
 * יחסי הגובה־רוחב הנתמכים. היחס **ננעל בקונטיינר** ולא נגזר מהתמונה,
 * כדי שהמקום יישמר לפני שהקובץ יורד. זה מה שמונע CLS — וב־Core Web
 * Vitals זה משפיע גם על הדירוג האורגני וגם על ציון הנחיתה בקמפיין.
 */
export const GALLERY_RATIOS = ["4/3", "3/2", "1/1", "4/5"] as const;
export type GalleryRatio = (typeof GALLERY_RATIOS)[number];

export interface GalleryImage {
  /** slug לטיני יציב. */
  id: string;

  /** קובץ ברירת המחדל. הגדול ביותר שמוגש בלי srcSet. */
  src: string;

  /**
   * ‎`srcset` מוכן, למשל `"/img/a-800.webp 800w, /img/a-1600.webp 1600w"`.
   * `null` ⇒ מוגש `src` בלבד. זה תקין, אבל בזבזני בנייד — ותנועה
   * בתשלום בקטגוריה הזאת היא ברובה נייד.
   */
  srcSet: Slot<string>;

  /**
   * המידות **בפיקסלים של הקובץ ב־`src`**. חובה, ושתיהן. הן ה־`width`
   * וה־`height` שנכתבים על ה־`<img>`; בלעדיהן הדפדפן אינו יודע לשריין
   * מקום, וכל תמונה שנטענת דוחפת את העמוד.
   */
  width: number;
  height: number;

  /**
   * טקסט חלופי בעברית. חובה. מתאר **מה רואים** — לא «קייטרינג מאמאמיה»
   * ולא מילות מפתח. תיאור מדויק הוא גם הנגישות וגם מה ש־Google Images
   * ומנועי תשובות קוראים.
   */
  alt: string;

  /**
   * כיתוב מוצג. `null` ⇒ אין כיתוב, וזו אפשרות תקינה ונפוצה.
   * כיתוב שנוקב בעיר, בשעה או במספר סועדים הוא עובדה עסקית — לא לכתוב
   * אותו בלי שהבעלים מסר אותו.
   */
  captionHe: Slot<string>;

  ratio: GalleryRatio;

  /**
   * **אירוע אמיתי שהעסק הזה הפיק.** `true` בלבד מציג את התמונה.
   *
   * לא סטוק, לא רנדר, לא צילום שנלקח מספק אחר, ולא «תמונה שמראה את
   * הסגנון». צילום אירוע הוא ראיה: הוא אומר «זה מה שיצא מהמטבח הזה».
   * `false` ו־`null` שקולים, ובכוונה — «לא בדקתי» אינו «כן».
   */
  shotAtRealEvent: boolean | null;

  /**
   * האם רואים בתמונה אנשים שניתן לזהות (אורחים, צוות, בעלי האירוע).
   * `true` ⇒ נדרשת רשומת הסכמה מלאה ב־`consent`, אחרת התמונה נחסמת.
   */
  showsPeople: boolean;

  /**
   * הסכמה לפרסום. נדרשת כש־`showsPeople === true`, וגם כשבעל האירוע
   * ביקש שהאירוע שלו לא יוצג. `null` ⇒ אין רשומה; תמונה בלי אנשים
   * עוברת בלעדיה, תמונה עם אנשים — לא.
   */
  consent: Slot<PublishConsent>;
}

/**
 * ריקה, וזה המצב התקין. הגלריה אינה מרונדרת.
 *
 * TODO(owner): צילומים מאירועים שהופקו בפועל. לכל תמונה: הקובץ, המידות
 * המדויקות, תיאור בעברית של מה רואים, והאם רואים אנשים — ואם כן,
 * אישור בכתב לפרסום.
 *
 * הנחיות צילום שמונעות עבודה כפולה:
 *   · אופקי (‎4/3 או ‎3/2) לרוב השולחן, אנכי (‎4/5) למנה בודדת.
 *   · לצלם בשני רוחבים לפחות (‎800 ו־‎1600) כדי שיהיה `srcSet`.
 *   · בלי לוגו צרוב ובלי מסגרת — העיצוב מוסיף את שלו.
 *   · שם קובץ לטיני, יציב, בלי רווחים.
 */
export const GALLERY: readonly GalleryImage[] = [];

/* ═══════════════════ בוררים ═══════════════════ */
/*
 * כל בורר מחזיר את המצב הריק בלי לזרוק ובלי להשלים. הקומפוננטה שואלת
 * את הבורר ולא את השדה — כדי שהתנאי «מה נחשב מוכן להצגה» ייכתב פעם
 * אחת ולא בחמישה מקומות.
 */

const isIsoDate = (v: unknown): v is string =>
  typeof v === "string" && /^\d{4}-\d{2}-\d{2}$/.test(v.trim());

/** האם רשומת ההסכמה שלמה ומאשרת בפועל. */
export function consentGranted(c: Slot<PublishConsent>): boolean {
  return filled(c) && c.granted === true && isIsoDate(c.grantedOn) && filled(c.evidenceHe);
}

/**
 * הביקורות, ורק כשכל מה שדרוש להצגה כנה קיים: דירוג בטווח, מונה חיובי,
 * קישור לפרופיל שאפשר לאמת מולו, וניסוח מה דורג. חסר אחד מהם — `null`,
 * והבלוק כולו נעלם. אין הצגת דירוג בלי קישור, ואין קישור בלי דירוג.
 */
export function googleReviews(): GoogleReviewProfile | null {
  const g = GOOGLE_REVIEWS;
  if (!filled(g)) return null;
  if (!Number.isFinite(g.ratingValue) || g.ratingValue <= 0 || g.ratingValue > 5) return null;
  if (!Number.isInteger(g.reviewCount) || g.reviewCount <= 0) return null;
  if (!filled(g.profileUrl) || !/^https:\/\//i.test(g.profileUrl)) return null;
  if (!filled(g.subjectHe)) return null;
  return g;
}

export const hasGoogleReviews = (): boolean => googleReviews() !== null;

/**
 * ההמלצות שמותר לפרסם. הסינון הוא על ההסכמה, ולא על טעם — המלצה בלי
 * אישור בכתב אינה מוצגת גם אם היא המחמיאה ביותר בקובץ.
 */
export function publishableTestimonials(limit?: number): Testimonial[] {
  const out = TESTIMONIALS.filter((t) => filled(t.quoteHe) && consentGranted(t.consent));
  return typeof limit === "number" && limit > 0 ? out.slice(0, limit) : out;
}

export const hasTestimonials = (): boolean => publishableTestimonials().length > 0;

/**
 * התמונות שמותר להציג. חמישה תנאים, וכולם נדרשים:
 * קובץ · מידות אמיתיות · תיאור חלופי · אירוע אמיתי · הסכמה כשיש אנשים.
 */
export function galleryImages(limit?: number): GalleryImage[] {
  const out = GALLERY.filter(
    (g) =>
      filled(g.src) &&
      Number.isFinite(g.width) &&
      g.width > 0 &&
      Number.isFinite(g.height) &&
      g.height > 0 &&
      filled(g.alt) &&
      g.shotAtRealEvent === true &&
      (g.showsPeople !== true || consentGranted(g.consent)),
  );
  return typeof limit === "number" && limit > 0 ? out.slice(0, limit) : out;
}

export const hasGallery = (): boolean => galleryImages().length > 0;

/**
 * האם יש באתר ולו ראיה אחת. `false` היום.
 *
 * זה מה שעמוד שואל לפני שהוא בונה «אזור הוכחה» עם כותרת: כותרת מעל
 * שלושה בלוקים שכולם החזירו null היא בדיוק החור בפריסה שהחוק אוסר.
 */
export const hasAnyProof = (): boolean =>
  hasGoogleReviews() || hasTestimonials() || hasGallery();

/* ═══════════════════ בדיקת שפיות ═══════════════════ */

/**
 * מאתרת את הטעויות שקורות כשמזינים ידנית. מחזירה רשימה ואינה זורקת:
 * קובץ נתונים שגוי אסור לו להפיל עמוד ללקוח.
 *
 * הבדיקות החשובות כאן אינן טכניות אלא משפטיות — פריט שנכתב **וסונן
 * החוצה בשקט** נראה למי שהזין אותו כאילו לא נשמר, והוא ינסה שוב בלי
 * ההסכמה. לכן כל סינון מדווח בשמו.
 */
export function validateProof(): string[] {
  const problems: string[] = [];

  const g = GOOGLE_REVIEWS;
  if (filled(g)) {
    if (!(g.ratingValue > 0 && g.ratingValue <= 5)) {
      problems.push(`ביקורות גוגל: דירוג ${g.ratingValue} מחוץ לטווח 1 עד 5`);
    }
    if (!Number.isInteger(g.reviewCount) || g.reviewCount <= 0) {
      problems.push("ביקורות גוגל: מונה הביקורות חייב להיות מספר שלם חיובי");
    }
    if (!filled(g.profileUrl) || !/^https:\/\//i.test(g.profileUrl)) {
      problems.push("ביקורות גוגל: חסר קישור https לפרופיל — בלי אימות אין הצגה");
    }
    if (!filled(g.subjectHe)) {
      problems.push("ביקורות גוגל: חסר `subjectHe` — מה בדיוק דורג. חובה, לא הערה.");
    }
    if (!isIsoDate(g.capturedOn)) {
      problems.push("ביקורות גוגל: `capturedOn` חייב להיות YYYY-MM-DD");
    }
  }

  const seen = new Set<string>();
  for (const t of TESTIMONIALS) {
    const at = `המלצה "${t.id || "(ללא מזהה)"}"`;
    if (!t.id) problems.push(`${at}: חסר מזהה`);
    else if (seen.has(t.id)) problems.push(`${at}: מזהה כפול`);
    else seen.add(t.id);

    if (!filled(t.quoteHe)) problems.push(`${at}: ציטוט ריק`);
    if (!consentGranted(t.consent)) {
      problems.push(`${at}: אין רשומת הסכמה מלאה (אישור, תאריך, היכן שמור) — אינה מוצגת`);
    }
  }

  const seenImg = new Set<string>();
  for (const img of GALLERY) {
    const at = `תמונה "${img.id || "(ללא מזהה)"}"`;
    if (!img.id) problems.push(`${at}: חסר מזהה`);
    else if (seenImg.has(img.id)) problems.push(`${at}: מזהה כפול`);
    else seenImg.add(img.id);

    if (!filled(img.src)) problems.push(`${at}: חסר קובץ`);
    if (!(img.width > 0 && img.height > 0)) {
      problems.push(`${at}: חסרות מידות אמיתיות — בלעדיהן העמוד קופץ בטעינה (CLS)`);
    }
    if (!filled(img.alt)) problems.push(`${at}: חסר טקסט חלופי`);
    if (img.shotAtRealEvent !== true) {
      problems.push(`${at}: לא הוצהרה כצילום מאירוע אמיתי — אינה מוצגת`);
    }
    if (img.showsPeople === true && !consentGranted(img.consent)) {
      problems.push(`${at}: רואים בה אנשים ואין רשומת הסכמה — אינה מוצגת`);
    }
    if (!GALLERY_RATIOS.includes(img.ratio)) {
      problems.push(`${at}: יחס לא מוכר "${img.ratio}"`);
    }
  }

  return problems;
}

/* אזהרה בפיתוח בלבד. בפרודקשן אין לוג ואין השפעה על הרינדור. */
if (import.meta.env?.DEV) {
  const problems = validateProof();
  if (problems.length > 0) {
    // eslint-disable-next-line no-console
    console.warn(`[proof.ts] ${problems.length} בעיות:\n · ${problems.join("\n · ")}`);
  }
}
